name: CI

on:
  push:
    branches: [master]
    paths-ignore:
      - '**.md'
  pull_request:
    branches: [master] 

jobs:
  test-frontend:
    name: Frontend
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Set up Go (for Wails bindings generation)
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache: true

      - name: Generate Wails bindings
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest && wails generate module

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Type check
        working-directory: frontend
        run: npx tsc --noEmit

      - name: Tests
        working-directory: frontend
        run: npm run test

  test-go:
    name: Go
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache: true

      - name: Generate Wails bindings
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest && wails generate module

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Build frontend (required for go:embed)
        working-directory: frontend
        run: npm ci && npm run build

      - name: Vet
        run: go vet ./...

      - name: Tests
        run: go test -p 4 ./...
